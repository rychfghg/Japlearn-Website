Place the current Android build in this folder with this exact filename:

JapLearn-latest.apk

The landing-page Android download button points to that file by default.
For an externally hosted APK, set VITE_ANDROID_APK_URL in Vercel instead.
